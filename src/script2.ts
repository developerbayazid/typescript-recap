const a: (number | string)[] = [];

a.push('3');

let b: string | number;

b = 5;
b = 'B';

let o: {
    name: string;
    age: number;
    isAdult: boolean;
};

o = {
    name: 'Bayazid',
    age: 23,
    isAdult: true,
};
